filebeat.inputs:
  - type: filestream
    paths:
#      - /logs/elastic.log
      - /logs/orderservice.log

output.elasticsearch:
  hosts: ["http://elasticsearch:9200"]

setup.kibana:
  host: "http://kibana:5601"